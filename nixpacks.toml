# Railway/Nixpacks — fuerza Python (aunque el repo tenga package.json) y arranca el
# Studio con --app-dir para que app:app resuelva a examples/studio/app.py.
providers = ["python"]

[start]
cmd = "uvicorn app:app --app-dir examples/studio --host 0.0.0.0 --port $PORT"
